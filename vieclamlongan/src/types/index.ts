export type SearchInputItem = {
    url: string;
    placeholder: string;
}

export type SearchInputMap = {
    [key: string]: SearchInputItem;
}

export type Banner = {
    url: string,
    alt: string
}